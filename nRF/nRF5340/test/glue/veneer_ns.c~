#include "arm_cmse.h"
typedef unsigned int    u32;
#define REG32(x) ((*(volatile u32 *)(x)))

__attribute__ ((cmse_nonsecure_entry))
unsigned ns_to_s_veneer(unsigned x)
{
    extern unsigned ns_to_s(unsigned);
    return ns_to_s(x);
}
